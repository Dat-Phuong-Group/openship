# Optional host port publishes for local `docker compose` (not for Dokploy).
# Usage:
#   docker compose -f docker-compose.yml -f docker-compose.local.yml up -d --build
services:
  api:
    ports:
      - "4000:4000"
  dashboard:
    ports:
      - "3001:3001"
  web:
    ports:
      - "3000:3000"
